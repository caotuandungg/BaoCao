apiVersion: apps/v1
kind: Deployment
metadata:
  name: dung-db-log-generator
  namespace: dung-lab
  labels:
    owner: dung
    project: dung-lab
    purpose: log-testing
    component: db
spec:
  replicas: 3
  selector:
    matchLabels:
      app: dung-db-log-generator
  template:
    metadata:
      labels:
        app: dung-db-log-generator
        owner: dung
        project: dung-lab
        purpose: log-testing
        component: db
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - dung-db-log-generator
            topologyKey: kubernetes.io/hostname
      containers:
      - name: db
        image: python:3.11-alpine
        command: ["python", "/scripts/db.py"]
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 100m
            memory: 128Mi
        volumeMounts:
        - name: scripts
          mountPath: /scripts
      volumes:
      - name: scripts
        configMap:
          name: dung-log-generator-scripts
